# 西华大学宿舍电费监控系统 - 使用指南

> **项目说明**：本项目针对西华大学一卡通宿舍用电小程序，用于监控宿舍电费使用情况。  
> **管理员QQ**：714085964

## 目录

1. [快速开始](#快速开始)
2. [环境配置](#环境配置)
3. [抓包获取认证信息](#抓包获取认证信息)
4. [邮件告警配置](#邮件告警配置)
5. [启动服务](#启动服务)
6. [使用说明](#使用说明)
7. [常见问题](#常见问题)

---

## 快速开始

### 环境要求

- **Python 3.8+**
- **Node.js 16+** 和 npm
- **MySQL 5.7+**
- **Git**（可选，用于代码更新）

### 快速部署步骤

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd dorm-power-guard-lite
   ```

2. **配置数据库**
   ```bash
   mysql -u root -p < backend/init_db.sql
   ```

3. **配置后端**
   ```bash
   cd backend
   pip install -r requirements.txt
   cp .env.example .env
   # 编辑 .env 文件，填入配置
   ```

4. **配置前端**
   ```bash
   cd frontend
   npm install
   ```

5. **启动服务**
   ```bash
   # 后端（在 backend 目录）
   python run.py
   
   # 前端（在 frontend 目录，新终端）
   npm run dev
   ```

6. **访问系统**
   - 前端：http://localhost:3000
   - 后端API：http://localhost:8000

---

## 环境配置

### 1. 数据库配置

编辑 `backend/.env` 文件：

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=dorm_power_guard
```

### 2. 爬虫配置（西华大学一卡通）

```env
# 系统地址
CRAWLER_BASE_URL=https://ecard.xhu.edu.cn
CRAWLER_API_BASE_URL=https://ecard.xhu.edu.cn/api

# 宿舍信息
CRAWLER_DORM_NUMBER=320  # 您的宿舍号
CRAWLER_ROOM_ID=5699     # 房间ID（通过抓包获取）

# 区域和楼栋信息（根据实际情况修改）
CRAWLER_AREA_ID=1        # 区域ID（1=郫都校区）
CRAWLER_YQ_ID=3          # 园区ID（3=德馨苑）
CRAWLER_BUILDING_ID=40-1 # 楼栋ID
CRAWLER_FLOOR_ID=3       # 楼层ID
CRAWLER_FACTORY_CODE=E014
CRAWLER_SIGN=qt
CRAWLER_ORG_ID=2
```

### 3. 认证信息配置

**重要**：需要通过抓包获取认证信息，详见[抓包获取认证信息](#抓包获取认证信息)章节。

```env
# 认证信息（通过抓包获取）
CRAWLER_OPENID=your_openid
CRAWLER_JSESSIONID=your_jsessionid
```

---

## 抓包获取认证信息

### 方法一：快速上手（推荐新手）

详细步骤请参考：**`抓包教程-快速版.md`**

简要流程：
1. 安装Charles抓包工具
2. 配置手机代理
3. 打开西华大学一卡通小程序
4. 查看请求，提取 `openid` 和 `JSESSIONID`
5. 配置到 `.env` 文件

### 方法二：详细教程

完整教程请参考：**`抓包教程-详细版.md`**

包含：
- 多种抓包工具使用方法
- 详细配置步骤
- 常见问题解决
- 认证信息说明

### 认证信息说明

- **openid**：用户唯一标识，通过URL参数传递
- **JSESSIONID**：会话ID，通过Cookie传递
- **有效期**：通常不会过期，小程序会保持登录状态

---

## 邮件告警配置

### 1. 获取QQ邮箱授权码

1. 登录QQ邮箱：https://mail.qq.com
2. 设置 → 账户 → 开启"POP3/SMTP服务"
3. 生成授权码（16位）

### 2. 配置邮件发送

编辑 `backend/.env` 文件：

```env
# 启用邮件告警
EMAIL_ENABLED=true

# QQ邮箱SMTP配置
EMAIL_SMTP_HOST=smtp.qq.com
EMAIL_SMTP_PORT=465
EMAIL_SMTP_USER=1270667498@qq.com
EMAIL_SMTP_PASSWORD=your_auth_code
EMAIL_FROM=1270667498@qq.com

# 默认接收邮箱（可选）
EMAIL_TO=
```

### 3. 配置告警规则

在前端页面：
1. 进入"告警规则"页面
2. 点击"编辑"
3. 开启"邮件告警"
4. 设置告警阈值（空调和照明）
5. 输入接收邮箱地址
6. 保存

**注意**：告警规则中配置的邮箱优先级高于全局配置的 `EMAIL_TO`。

详细配置请参考：**`邮箱配置和测试指南.md`**

---

## 启动服务

### Windows系统

#### 方式一：使用批处理文件（推荐）

```bash
# 后端
cd backend
start.bat

# 前端（新终端）
cd frontend
npm run dev
```

#### 方式二：手动启动

```bash
# 后端
cd backend
python run.py

# 前端（新终端）
cd frontend
npm run dev
```

#### 方式三：注册为Windows服务（24小时运行）

```powershell
# 以管理员身份运行PowerShell
cd backend
.\install_windows_service.ps1
```

### Linux/Mac系统

```bash
# 后端
cd backend
python3 run.py

# 前端（新终端）
cd frontend
npm run dev
```

### 云服务器部署

详细步骤请参考：**`程序持续运行部署指南.md`**

---

## 使用说明

### 1. 监控面板

- **功能**：查看当前电费余量、历史趋势图
- **操作**：点击"重新获取"按钮手动刷新数据
- **显示内容**：
  - 空调余量（度）
  - 照明余量（度）
  - 用电量趋势图

### 2. 电费记录

- **功能**：查看历史电费记录
- **显示内容**：
  - 记录时间
  - 空调余量和用电量
  - 照明余量和用电量

### 3. 告警规则

- **功能**：配置告警阈值和接收邮箱
- **配置项**：
  - 空调告警阈值（度）
  - 照明告警阈值（度）
  - 邮件告警开关
  - 接收邮箱地址（多个用逗号分隔）
  - QQ告警开关（可选）

### 4. 告警日志

- **功能**：查看历史告警记录
- **显示内容**：
  - 告警时间
  - 告警类别（空调/照明）
  - 告警状态（成功/失败）

### 5. 定时任务

系统默认在以下时间点自动获取电费数据：
- 08:00
- 12:00
- 18:00
- 22:00

可在 `.env` 文件中修改 `SCHEDULER_HOURS` 配置。

---

## 常见问题

### Q1: 如何获取认证信息？

**A**: 使用抓包工具（Charles/Fiddler）抓取小程序请求，提取 `openid` 和 `JSESSIONID`。详细教程请参考 `抓包教程-快速版.md`。

### Q2: 认证信息会过期吗？

**A**: 通常不会过期，小程序会保持登录状态。如果过期，需要重新抓包获取。

### Q3: 邮件发送失败怎么办？

**A**: 
1. 检查QQ邮箱授权码是否正确（不是QQ密码）
2. 检查网络连接
3. 查看后端日志：`backend/logs/` 目录
4. 参考 `邮箱配置和测试指南.md`

### Q4: 如何修改抓取频率？

**A**: 编辑 `backend/.env` 文件中的 `SCHEDULER_HOURS`，例如：
```env
SCHEDULER_HOURS=8,12,18,22  # 每天8点、12点、18点、22点
```

### Q5: 如何部署到云服务器？

**A**: 详细步骤请参考 `程序持续运行部署指南.md`。

### Q6: 如何更新代码？

**A**: 详细步骤请参考 `代码更新和维护指南.md`。

### Q7: 数据存储在哪里？

**A**: 所有数据存储在本地MySQL数据库中，不会上传到第三方服务器。

### Q8: 如何备份数据？

**A**: 
```bash
# 备份数据库
mysqldump -u root -p dorm_power_guard > backup.sql

# 恢复数据库
mysql -u root -p dorm_power_guard < backup.sql
```

---

## 相关文档

- **项目说明**：`项目说明.md`
- **抓包教程**：`抓包教程-快速版.md`、`抓包教程-详细版.md`
- **部署指南**：`程序持续运行部署指南.md`
- **代码更新**：`代码更新和维护指南.md`
- **邮件配置**：`邮箱配置和测试指南.md`
- **技术文档**：`源代码技术文档.md`

---

## 联系与支持

- **管理员QQ**：714085964
- **项目用途**：仅供西华大学学生个人使用
- **数据安全**：所有数据存储在本地，不会上传到第三方服务器

---

**注意**：本项目仅用于学习和个人使用，请遵守西华大学相关规定，不得用于商业用途。
